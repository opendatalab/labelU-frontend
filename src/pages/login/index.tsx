import React from 'react'
import currentStyles from './index.scss';
const Login = ()=>{
    return (<div className = {currentStyles.outerFrame}>
        <div className = {currentStyles.content}>this is login</div>
    </div>)
}

export default Login;